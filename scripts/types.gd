class_name Types
# this file defines lightweight types (and enums)
# it implicitly extends RefCounted

enum DroppableTypes {VANISHING, GRIDSNAP, FREEDROP, GRID}

enum MachineTypes {ALL, MIXING, BAKING}
